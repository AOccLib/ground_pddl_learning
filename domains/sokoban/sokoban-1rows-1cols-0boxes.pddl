(define (problem sokoban-1rows-1cols-0boxes)
    (:domain sokoban-untyped)
    (:objects
    pos0_0
    )
    (:init
       (arm_empty)
       (location pos0_0)
       (clear pos0_0)
       (at pos0_0)
    )
    (:goal (and
       (at pos0_0)
    )))
